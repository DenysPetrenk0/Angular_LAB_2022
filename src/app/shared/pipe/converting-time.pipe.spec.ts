import { ConvertingTimePipe } from './converting-time.pipe';

describe('ConvertingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
