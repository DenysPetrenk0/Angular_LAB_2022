import { DateTypeDirective } from './date-type.directive';

describe('DateTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTypeDirective();
    expect(directive).toBeTruthy();
  });
});
